namespace PensionModel
{
    class Program
    {
        static void Main(string[] args)
        {
            App.App.Run(args);  // i.e. (since there is no using directive for App) run PensionModel.App.App.Run
        }
    }
}
